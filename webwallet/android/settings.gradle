pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // <— ini saja
    repositories {
        google()
        mavenCentral()
    }
}

// Nama root opsional (boleh ada/enggak)
// rootProject.name = "android"

include ':app'
apply from: 'capacitor.settings.gradle' // ini akan include :capacitor-android & :capacitor-cordova-android-plugins
